# SPEC: Airbnb 房源健康诊断系统 — 需求规格 v0.4

> **本文档只描述「产品做什么、用户看到什么、什么算对」。技术实现（数据结构、算法、UI 库、部署）由实现方决定。**
>
> 版本：v0.4 · 日期：2026-05-27 · 作者：Yuan Zhang · 状态：待 Coding Agent 实现
>
> **v0.4 变更**（基于实测 airbnb.jp 数据结构 + 业务方向调整）：
> - **语言**：UI 全日语、AI 报告日语、PDF 日语、邮件日语（原 v0.3 中文为主 → 全部改日）
> - **B7 描述完整性**：改为"描述总长度 + 关键章节正则匹配"评分（Airbnb 实测返回单块 HTML，不分 7 子字段）
> - **A5 Quality Status**：明确 Airbnb 不公开八档原始数据，demo 阶段按评分推算并标注"参考值"
>
> **v0.3 变更**：
> - C1 趋势图：明确"近一年评分"为 mock 数据（无法回溯真实历史）
> - F7 周报：demo 阶段不实现定时器，仅保留"立即测试发送"按钮 + UI mock 文案
> - §6 / §7 / §8 同步更新

---

## 1. 产品是什么

一个 web 应用。用户粘贴一个 Airbnb 房源 URL，系统抓取该房源的公开信息，输出"5 维度健康诊断 + 综合评分 + AI 改进建议 + 预警邮件"。

**用户**：SOZONEXT 内部运营（demo 阶段主要是老板看效果）。

**用户场景**：
- 老板想快速判断"这套房源现在健康吗"，30 秒内得到答案
- 运营想知道哪些房源出问题、问题在哪、怎么改
- 评分跌到危险线时系统主动提醒，而不是被动等人发现

---

## 2. 使用流程

1. 用户打开网页
2. 粘贴一个 Airbnb 房源 URL，点 "诊断"
3. 系统抓取该房源公开数据（标题、描述、照片、设施、评论、评分）
4. 等待 10-30 秒（期间显示进度提示）
5. 看到诊断结果页：
   - 顶部：综合评分卡（字母 + 颜色）
   - 中部：5 个维度的细化分析
   - 旁边或下方：AI 生成的改进建议报告，可下载 PDF
6. 如果评分低于阈值，系统自动发送预警邮件
7. 系统每周一早上自动生成全部诊断过的房源摘要，发送给运营邮箱

---

## 3. 功能详细规格

### 3.1 综合评分展示

#### A1 · 字母评级（A / B / C / D）

**功能描述**：把房源的整体健康状况用 1 个字母概括，让用户 1 秒判断状态。

**用户看到**：页面最显著位置一个大评分卡，主元素是大号字母（要足够大，远距离也能看清）。字母下方显示对应的百分数（如 78 分），不抢眼。

**业务规则**：
- 综合分由 5 个维度（写真 / 标题 / 描述 / 设施 / 评论）的子评分平均得出（demo 阶段简单等权）
- 评级映射：
  - 90-100 分 → A
  - 75-89 分 → B
  - 60-74 分 → C
  - 0-59 分 → D

**验收条件**：
- 字母正确显示
- 评分卡是页面视觉焦点（最大、最醒目的元素）
- 若评分缺失，显示一个灰色"?"

---

#### A4 · 红 / 黄 / 绿色阶

**功能描述**：评分卡的颜色直接传达健康状态，不需要读字母。

**用户看到**：评分卡的背景色或字母色随评级变化。
- A 级 → 绿色（安心）
- B 级 → 黄绿色（基本良好）
- C 级 → 黄色（需关注）
- D 级 → 红色（预警）

**验收条件**：
- 4 种颜色对比明显，色盲用户也能区分（建议红绿组合时加图标辅助）
- 颜色和字母同时变化

---

#### A5 · Quality Status 八档阶梯

**功能描述**：在主评分卡下方显示 Airbnb 官方定义的 8 档质量状态，让用户知道"离被下架还有多远"。

**用户看到**：横向 8 个小方块依次排列，当前所处档位高亮，其他档位灰显。下方一行文字解释当前档位含义。

**业务规则（8 档由健康到危险）**：
1. Good — 无质量问题
2. Educate — 有 1 个质量提示
3. Warn — 多个质量问题，需重视
4. Probation — 多次问题，有下架风险
5. Additional Warn — 累积过多
6. Pending Removal — 30 天后下架
7. Suspended — 已暂停
8. Removed — 已下架

**⚠️ 数据可得性说明**：Airbnb 公开 PDP 数据**不暴露**这 8 档原始状态。demo 阶段按总评分推算：
- ≥ 4.8 → Good
- 4.5–4.79 → Educate
- 4.0–4.49 → Warn
- 3.5–3.99 → Probation
- 3.0–3.49 → Additional Warn
- < 3.0 → Pending Removal
- "Suspended/Removed" 公开页直接抓不到，永不展示

UI 上需标注"※ Airbnb の内部判定とは異なる参考値です"（小字注解，避免误导）。

**验收条件**：
- 8 档全部可见
- 当前档位有视觉高亮（颜色、加粗、填充等任意方式）
- 下方显示当前档位的一句话解释（日语）
- 必须有"参考值"日文小字标注
- 数据缺失时默认显示 "Good"

---

#### A7 · "再提高 N 分升档"提示

**功能描述**：让用户知道距离更高一级还差多少，鼓励改进。

**用户看到**：评分卡右下角一行小字，例如"再提高 12 分升至 A 级"。已是 A 级时显示"已达最高等级"。

**验收条件**：
- 任何评分输入下都能正确显示
- 字号明显小于主评分（次要信息）

---

### 3.2 五维度分析

整体布局：评分卡下方一行 5 个维度卡片（写真 / 标题 / 描述 / 设施 / 评论），每个卡片可点击展开看细节。

#### B1 · 写真数量评估

**功能描述**：判断该房源照片数量是否充足。

**用户看到**：写真卡片显示总照片数 + 状态评价 + 改进建议。

**业务规则**：
- 少于 5 张 → "❌ 不足"（Airbnb 政策最低要求）
- 5-9 张 → "🟡 良好"
- 10-19 张 → "🟢 充足"
- 20 张及以上 → "⭐ 丰富"

**验收条件**：
- 总数显示正确
- 状态徽章和文案对应业务规则
- 不足时给出明确的改进建议（"建议增加到至少 X 张"）

---

#### B2 · 封面照片合适度

**功能描述**：评估当前 cover 照片（第一张展示给客人的图）是否是最吸引人的内容。

**用户看到**：cover 照片的缩略图 + 一句评价。

**业务规则**：
- Cover 照片若是客厅、卧室、外景、整体空间 → "✅ 合适"（这些是首图最佳选择）
- Cover 是浴室、厨房细节、单一设施特写 → "⚠️ 一般"（不是首选，但可接受）
- 无法判断类别 → "⚠️ 无法判断"

**验收条件**：
- 显示 cover 缩略图
- 状态评价对应业务规则
- "⚠️" 状态时给出建议（如"建议将客厅/外景照片设为首图"）

---

#### B3 · 房间类别覆盖

**功能描述**：判断照片是否覆盖了房源的主要区域。

**用户看到**：5 个图标（卧室 / 厨房 / 卫浴 / 客厅 / 外景），有照片的高亮，缺失的灰显，下方列出缺失的类别。

**业务规则**：5 类房间各至少有 1 张照片为完整覆盖；少 1-2 类为"基本覆盖"；少 3 类以上为"严重缺失"。

**验收条件**：
- 5 个图标全部显示
- 覆盖/缺失状态正确
- 显示覆盖比例（如 "4/5"）
- 缺失时明确列出"缺哪几类"

---

#### B6 · 标题 SEO 关键词分析（占位）

**功能描述**：本功能完整逻辑留到下个版本。当前只在 UI 上"占位"，让用户知道这块未来会有。

**用户看到**：标题维度卡片内显示若干关键词，每个带一个绿/黄/红勾叉，下方明确标注"⏳ 功能开发中，将在 v1 实装"。

**验收条件**：
- 视觉上看起来"已经存在"
- 但不连接任何后端逻辑
- 必须有"开发中"的明显标识，防止用户误以为是真实结果

---

#### B7 · 描述完整性

**功能描述**：判断房源描述的"长度 + 关键章节覆盖度"是否充足。

**用户看到**：
- 描述总字数 + 评级（不足 / 良好 / 充足）
- 6 个关键章节覆盖度勾选：寝室 / リビング / キッチン / バスルーム / アクセス・交通 / 周辺・観光
- 缺失章节给出具体补充建议

**业务规则**：
- Airbnb 公开 PDP 描述实测返回单块 HTML，**没有标准 7 子字段**。本项改为按关键词章节匹配：
  - 描述长度 ≥ 800 字 + 命中 ≥ 5 个章节 → 充足（满分）
  - 长度 400–799 字 或 命中 3–4 章节 → 良好
  - 长度 < 400 字 或 命中 ≤ 2 章节 → 需改进
  - 完全为空 → 严重问题（最低分）
- 章节匹配用日语关键词正则：`寝室|ベッドルーム` / `リビング` / `キッチン` / `バスルーム|浴室|風呂` / `駅|アクセス|交通` / `周辺|観光|スポット`

**验收条件**：
- 描述字数显示正确
- 6 个章节图标全部列出，命中/未命中状态准确
- 缺失章节给出"描述に〇〇を追加してください"日文建议

---

#### B8 · 多语言覆盖

**功能描述**：判断该房源支持多少种语言的描述，重点检查 SOZONEXT 主要客源市场对应的语种。

**用户看到**：现有语言徽章 + 缺失语言提示。

**业务规则**：
- 必须有 JA（日语，本地市场基础）
- 必须有 EN（英文，国际通用）
- 加分项：ZH-CN（中文，重要客源）、KO（韩语）
- 缺日语或英文 → "⚠️ 基礎不足"

**⚠️ 数据可得性说明**：Airbnb 公开 PDP 单次只返回当前 locale 描述。demo 阶段通过追加 `?locale=` query 或切换 `_set_bev_on_new_domain` cookie 重抓 2–4 次判定语种存在性；超时容忍：抓不到时按"未提供"处理并标注"未確認"。

**验收条件**：
- 显示当前已有的所有 locale
- 缺失关键语种时高亮提示
- 给出"建议补充 X 语种"的提示

---

#### B10 · 设施与描述一致性 ⭐ 核心差异化

**功能描述**：判断房源在 Airbnb 上标注的设施，是否在描述文字中也提到了。如果某个设施没有在描述里 mention，房客容易觉得"被忽悠"或"看不到这个设施信息"。

**用户看到**：一个表格或列表，左侧是设施名（如 Wi-Fi、空调、洗衣机），右侧标注"描述中是否提到"。提到的显示一段引用句段；没提到的标"⚠️ 建议补充描述"。

**业务规则**：
- 设施被描述提到 = 一致
- 设施未被描述提到 = 不一致，建议补充
- 一致比例越高，本维度评分越高

**验收条件**：
- 列出该房源所有标注存在的设施
- 每个设施都有"是否一致"判定
- 不一致时给出具体建议
- 整体显示"X/Y 一致" 总结

---

#### B12 · 高频差评关键词 AI 抽取 ⭐ 核心 wow 点

**功能描述**：从所有评论中用 AI 提取出最常被抱怨的几个主题/关键词，让运营一眼看到"客人最常吐槽什么"。

**用户看到**：评论维度卡片内列出 Top 5 高频负面词，每个带：
- 关键词本身（2-4 字短词，如"脏"、"WiFi 慢"）
- 出现次数
- 一条典型评论引用（让用户感受具体抱怨内容）

如果评论数太少（少于 3 条）或没有负面词，显示对应说明文字。

**业务规则**：
- 只看负面/抱怨主题，正面词不抽取
- 同义抱怨合并（"汚い"、"清潔感がない"、"カーペットに髪の毛" → 一个主题）
- 评论数不足 → 显示"レビュー数が少ないため参考程度"
- 完全无差评 → 显示"✅ 高頻度の否定キーワードは検出されませんでした"

**验收条件**：
- Top 5 列出（不足 5 个就有几个列几个）
- 每个关键词都有次数 + 引用
- **关键词输出统一为日文**（原评论可能是日英中混合，由 AI 翻译/归纳为日文短语；引用句保留原文）
- 评论极少或无差评时有合理的兜底文案（日文）

---

### 3.3 趋势

#### C1 · 评分历史曲线

**功能描述**：让用户看到该房源的评分变化趋势。

**用户看到**：一张简单的折线图，X 轴时间，Y 轴评分（1-5 星）。当前 demo 阶段，"近一年评分"为 **mock 数据**（去年的真实评分无法回溯获取），"当前评分"为本次诊断真实结果。两点连成一条线。曲线下方明确标注"评分历史（近一年为示例数据，下个版本接入真实历史）"。

**业务规则**：
- demo 阶段：始终画 2 个点的折线，"近一年"点使用 mock 值（与当前评分有合理小幅差异，体现"趋势"观感）
- 必须在图表附近显式标注 mock 字样，不让用户误以为是真实历史
- v1 阶段：替换为真实历史数据；若无历史，只画当前 1 个点，配文字"需积累数据"

**验收条件**：
- 图表渲染正确（2 个点 + 折线）
- "近一年为示例数据" 的 mock 标注明显，用户不会误以为是真实历史

---

#### C4 · 上次诊断 vs 本次的对比

**功能描述**：显示评分相比上次诊断的变化。

**用户看到**：评分附近一个小箭头 + 变化量。
- 升 → "↑ +0.2"，绿色
- 降 → "↓ -0.5"，红色
- 平 → "= 持平"，灰色

**业务规则**：
- 首次诊断 → 显示"首次诊断"，不显示箭头
- 有上次数据 → 计算差值，按上面方向显示

**验收条件**：
- 三种方向（升 / 降 / 平 / 首次）都能正确显示
- 颜色对应方向（升绿、降红、平灰）

---

### 3.4 AI 综合诊断报告（E）

**功能描述**：用 AI 自动生成一份完整的房源诊断报告，覆盖总评、5 维度分析、改进建议。报告应该是"运营拿着就能干活"的级别。

**用户看到**：页面侧边或下方有一片可滚动的长文，顶部一个"📄 下载 PDF"按钮。

**报告必须包含的章节（按这个顺序）**：

1. **报告头部** — 房源名、诊断时间、综合评分
2. **总评** — 1-2 段自然语言概括整体状况（强项、弱项）
3. **5 维度分析** — 每个维度一节，写当前状态 + 1-2 条具体建议
4. **Top 3 改进建议（最高优先级）** — 从所有建议里选出对评分提升最显著的 3 条，每条要包含：
   - 当前问题（一句话描述）
   - 改进动作（具体可执行）
   - 预期收益（一句话）
5. **风险提示** — 如有低分维度或严重差评，列出主要风险；否则写"暂无显著风险"

**风格要求**：
- **日本語**（敬体「です・ます」調、ビジネス文書ライク）
- 直接、可操作（避免"ユーザー体験を向上させましょう"这种空话；要写"説明文に Wi-Fi の速度を明記してください"）
- 每条建议都要能落地，运营看完知道下一步动作

**PDF 下载**：
- 点击按钮后浏览器自动下载 PDF
- 文件名包含房源标识和日期
- **PDF 全日文显示正常，不乱码**（日文字体 NotoSansJP 内嵌）
- 排版整洁（标题、段落、列表可读）

**验收条件**：
- 包含上述 5 个章节
- Top 3 建议是真的"Top 3"（不是把 5 条全堆上去）
- 文案是 actionable 的，不是空话
- PDF 下载能成功，打开正常显示

---

### 3.5 邮件预警

#### F1 · 评分低于阈值自动邮件

**功能描述**：当某次诊断结果综合分低于 60 时，系统自动发送预警邮件给运营，包含问题概述和报告链接。

**用户看到**：
- 在结果页底部一段状态文字
  - 评分 < 60：显示"⚠️ 已触发预警邮件 → 已发送至 [邮箱]"
  - 评分 ≥ 60：显示"✅ 评分健康（X 分 > 60），未触发预警"
- 邮件目标地址可在 UI 上配置（demo 阶段用一个测试邮箱即可，禁止发到真实运营邮箱）

**邮件内容必须包含**（日本語本文）：
- 主题：「⚠️ 物件アラート · {房源名} · 評価 {score}」
- 当前评分
- 与上次的变化（来自 C4）
- Top 3 问题（来自 AI 报告的 Top 3 建议）
- 报告页链接

**业务规则**：
- 阈值 60 分，可由配置调整
- 同一房源同一次诊断只触发一次邮件（避免重复打扰）
- 评分 ≥ 60 不发邮件

**验收条件**：
- 评分 < 60 时自动发出邮件
- 评分 ≥ 60 时不发邮件
- 邮件包含上述全部内容
- 页面有清晰的"已发送/未触发"状态
- demo 中可以验证邮件确实送达（用测试邮箱收件箱看到）

---

#### F7 · 每周摘要邮件

**功能描述**：每周一早上自动汇总过去 7 天所有诊断过的房源，生成一封"周报"邮件发给运营。

**用户看到**：
- 结果页底部一行说明："下次自动发送时间：下周一 09:00"
- 一个"🧪 立即测试发送"按钮，点击立刻发送一封 demo 周报，方便老板看效果

**邮件内容必须包含**（日本語本文）：
- 主题：「SOZONEXT 物件ヘルス週次サマリー · {日付}」
- 本周诊断房源总数
- 评级分布（A/B/C/D 各多少个）
- 需重点关注的 Top 3 房源（评分最低的前 3 个），每个写：
  - 房源名
  - 评级 + 分数
  - 主要问题（一句话，日语）

**业务规则**：
- **demo 阶段不实现真实定时机制**（仅原型展示该能力，定时器移到 v1）
- 仅保留"🧪 立即测试发送"按钮进行演示
- 页面文字"下次自动发送时间：下周一 09:00"为 mock 展示文案
- demo 阶段如果只诊断过 1 个房源，其他统计可用合理的展示用数据填充（明确标注是 demo 数据）

**验收条件**：
- "立即测试发送"按钮能触发邮件
- 邮件格式符合上述结构
- 页面有"下次发送时间"展示文案（mock，不连定时器）
- ~~实际定时机制能运行（每周一 9 点真发出去）~~ → 移至 v1

---

## 4. 整体页面布局期望（不约束像素）

- **评分卡**应该是页面最醒目的元素，第一眼看见
- **5 维度卡片**横向或网格排列，紧邻评分卡
- **AI 报告**占较大版面（侧边或下方），方便阅读
- **趋势图**单独一块，不喧宾夺主
- **邮件预警状态**放在结果页底部，是辅助信息
- 桌面端宽屏可分两栏；窄屏自适应为单栏纵向

具体配色、字号、间距由实现方决定，但需符合：
- 评分卡要够大够显眼
- 颜色变化要清晰可辨
- 文字可读，避免拥挤

---

## 5. 边界与异常处理

| 场景 | 期望行为 |
|---|---|
| 用户输入的 URL 不是 Airbnb 房源链接 | 表单立即提示"请输入有效的 Airbnb 房源 URL" |
| 抓取失败（房源不存在 / 网络问题 / 被屏蔽） | 显示明确错误信息，建议用户检查 URL 或稍后重试。**不要 fallback 到假数据** |
| 评论数为 0 | 评论维度显示"暂无评论"；负面词分析显示"无可分析数据"；评论子分给中性值 |
| 评论数少于 3 | 负面词分析显示"评论数不足，结果仅供参考" |
| 近一年评分数据缺失 | C1 曲线只画 1 个点，配文字"需积累数据"；C4 显示"首次诊断" |
| 描述完全为空 | 描述维度显示"描述完全为空，建议立刻补充"，分数为最低 |
| AI 分析调用失败 | 重试一次；仍失败则报告该 section 显示"AI 分析暂时不可用，请稍后刷新" |
| 邮件发送失败 | UI 显示具体失败原因，但不阻断诊断流程 |

---

## 6. 明确不做（防止 scope creep）

- ❌ 用户登录、账号管理、权限系统
- ❌ 多房源批量诊断、portfolio dashboard
- ❌ 多平台聚合（Booking、VRBO）
- ❌ 飞书 / Slack 集成（先邮件即可）
- ❌ 工单系统（派工、状态机、完成度追踪）
- ❌ AI 一键修改房源
- ❌ 给房东直接发送报告
- ❌ 评分公式 calibration（demo 用本文档定义的简单规则即可）
- ❌ 真正的历史数据持久化（demo 阶段每次诊断独立即可）
- ❌ 周报定时发送（demo 阶段仅手动触发"立即测试发送"按钮）
- ❌ C1 趋势图接入真实近一年评分（demo 阶段 mock 一个数据点）

---

## 7. 实现优先级（时间紧时按这个顺序砍）

按下列顺序砍：

1. **核心绝不能砍**：A1 + A4（评分卡） · 至少 1 个维度卡片有真实分析 · AI 报告 · F1 预警邮件
2. 可砍：A5 八档阶梯（保留 quality_status 文字描述即可）
3. 可砍：A7 升档提示
4. 可砍：B6 SEO 占位
5. 可砍：C1 历史曲线 → demo 已是 mock 形式（2 个点折线），再砍则改为一行文字"近一年 4.5 → 当前 4.7"
6. F7 周报：demo 已仅保留"立即测试发送"按钮 + UI mock 文案，定时器在 v1 实装；再砍则按钮点击显示"开发中"
7. 可砍：PDF 下载（保留页面内 markdown 即可）

**核心 demo 必须能跑完整链路：输入 URL → 评分 → 至少 1 个维度细节 → AI 报告 → 预警邮件状态可见。**

---

## 8. 验收标准 — demo 怎么算"成功"

实现完成后，演示流程：

1. 打开网页
2. 粘贴一个 Airbnb 房源 URL
3. 点 "诊断"
4. 等待几十秒
5. 看到完整诊断结果（评分卡 + 5 维度 + AI 报告 + 趋势图）
6. 点击下载 PDF，成功获得 PDF 文件
7. 如果该房源评分 < 60，看到"已发送预警邮件"状态，且测试邮箱真的收到邮件
8. 点击"立即测试发送周报"，测试邮箱收到摘要邮件（demo 阶段无定时器，依赖此按钮触发）

**这个流程一气呵成跑通 = demo 成功。**

---

## 9. 信息密度与文案风格

整个产品的文案应该：
- **日本語を主とする**（UI ラベル / ボタン / トースト / 説明文すべて日本語）
- 关键英文术语保留（Wi-Fi、A/B/C/D、Quality Status 等专有名词不强译）
- 敬体「です・ます」調、丁寧だが過度に冗長にしない
- 直接、可操作（避免空话）
- 不堆砌 emoji，但关键状态点 emoji 强化（✅ ⚠️ ❌ 🟢 🟡 🔴）
- 评分卡用简短表达（数字 + 字母 + 状态词），不写整段文字
- AI 报告可以详细，但每条建议都要 actionable

**典型 UI 文案样例**：
- 表单按钮："診断する"
- 加载中："物件データを取得中…" / "AI が分析中…" / "レポート生成中…"
- 评分卡副文案："A 級（健全）" / "C 級（要注意）"
- 升档提示："あと 12 点で A 級にアップ"
- 错误："無効な Airbnb URL です。確認してください"

---

**End of v0.2**
